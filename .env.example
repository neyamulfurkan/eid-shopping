# =============================================================================
# EID E-COMMERCE PLATFORM — ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and fill in all values before running the app.
# Never commit your .env file to version control — it contains secrets.
# =============================================================================


# -----------------------------------------------------------------------------
# DATABASE
# -----------------------------------------------------------------------------

# PostgreSQL connection string used by Prisma ORM for all database operations.
# Format:  postgresql://USER:PASSWORD@HOST:5432/DATABASE_NAME
# Local:   postgresql://postgres:password@localhost:5432/eid_store
# Neon:    Copy the "pooled connection" string from your Neon project dashboard.
# Supabase: Use the "Transaction pooler" URI from Project Settings > Database.
DATABASE_URL="postgresql://postgres:password@localhost:5432/eid_store"


# -----------------------------------------------------------------------------
# NEXTAUTH (Authentication)
# -----------------------------------------------------------------------------

# The full base URL of this application.
# Development: http://localhost:3000
# Production:  https://yourstore.vercel.app  (must match your actual domain — no trailing slash)
NEXTAUTH_URL="http://localhost:3000"

# Secret key used to sign and encrypt NextAuth.js JWT session tokens.
# Generate a secure value with:  openssl rand -base64 32
# Never reuse this value across projects. Never commit it to git.
NEXTAUTH_SECRET="replace-this-with-output-of-openssl-rand-base64-32"


# -----------------------------------------------------------------------------
# CLOUDINARY (Image Storage & Delivery)
# -----------------------------------------------------------------------------

# Your Cloudinary cloud name.
# Found on the Cloudinary Dashboard home page under "Account Details".
# Example: my-store-cloud
CLOUDINARY_CLOUD_NAME="your-cloudinary-cloud-name"

# Cloudinary API Key.
# Found in Cloudinary Dashboard > Settings > Access Keys.
# Used for server-side authenticated upload and delete operations.
CLOUDINARY_API_KEY="your-cloudinary-api-key"

# Cloudinary API Secret.
# Found in Cloudinary Dashboard > Settings > Access Keys.
# Used for server-side signed uploads. NEVER expose this to the browser.
CLOUDINARY_API_SECRET="your-cloudinary-api-secret"

# Public Cloudinary cloud name exposed to the browser (NEXT_PUBLIC_ prefix).
# Set this to the same value as CLOUDINARY_CLOUD_NAME above.
# Used by client-side code to construct optimized Cloudinary image URLs
# without requiring the secret API credentials.
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME="your-cloudinary-cloud-name"